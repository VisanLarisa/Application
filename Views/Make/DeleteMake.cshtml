@model IEnumerable<Application.Models.Make>

@{
    ViewBag.Title = "DeleteMake";
}

<h3>Are you sure you want to delete this?</h3>
<div>
    @using (Html.BeginForm("Delete", "Make", FormMethod.Post))
    {
        @Html.HiddenFor(e => e.MakeId)
        <h4>Delete Make</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.BrandName)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.BrandName)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.OriginCoutry)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.OriginCoutry)
            </dd>

        </dl>
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to Make List", "Index")
        </div>
    }
</div>

